```csharp
using Microsoft.Data.SqlClient;
using System;
using System.Data;

namespace CRUD_Disconnected
{
    internal class Program
    {
        // Connection String (Fixed typo TrustedServerCertificate)
        static string sqlstring = @"Server=.\SQLEXPRESS; Database=EmployeeDB; Trusted_Connection=True; TrustServerCertificate=True;";

        static void Main(string[] args)
        {
            Console.WriteLine("Enter the process name: insert, update, delete");
            string process = Console.ReadLine().ToLower();

            int result = 0;

            if (process == "insert")
            {
                result = InsertRecord();
            }
            else if (process == "update")
            {
                result = UpdateRecord();
            }
            else if (process == "delete")
            {
                result = DeleteRecord();
            }
            else
            {
                Console.WriteLine("Wrong process name.");
            }

            if (result > 0)
                Console.WriteLine("Operation Successful.");
            else
                Console.WriteLine("Operation Failed.");

            Console.WriteLine("\nAll Records:\n");

            ShowAllRecords();

            Console.WriteLine("\nPress any key to exit...");
            Console.ReadKey();
        }

        // ================= SHOW ALL RECORDS =================

        static void ShowAllRecords()
        {
            using SqlConnection con = new SqlConnection(sqlstring);

            SqlDataAdapter adapter =
                new SqlDataAdapter("SELECT * FROM Employee", con);

            DataSet ds = new DataSet();

            adapter.Fill(ds, "Employee");

            foreach (DataRow row in ds.Tables["Employee"].Rows)
            {
                Console.WriteLine(
                    $"Id: {row["Id"]}, Name: {row["Name"]}, Role: {row["Role"]}, Email: {row["Email"]}, Salary: {row["Salary"]}");
            }
        }

        // ================= INSERT =================

        static int InsertRecord()
        {
            Console.WriteLine("Enter Name:");
            string name = Console.ReadLine();

            Console.WriteLine("Enter Role:");
            string role = Console.ReadLine();

            Console.WriteLine("Enter Email:");
            string email = Console.ReadLine();

            Console.WriteLine("Enter Salary:");
            int salary = Convert.ToInt32(Console.ReadLine());

            using SqlConnection con = new SqlConnection(sqlstring);

            SqlDataAdapter adapter =
                new SqlDataAdapter("SELECT * FROM Employee", con);

            DataSet ds = new DataSet();

            adapter.Fill(ds, "Employee");

            DataRow newRow = ds.Tables["Employee"].NewRow();

            newRow["Name"] = name;
            newRow["Role"] = role;
            newRow["Email"] = email;
            newRow["Salary"] = salary;

            ds.Tables["Employee"].Rows.Add(newRow);

            SqlCommand insertCommand = new SqlCommand(
                "INSERT INTO Employee(Name, Role, Email, Salary) VALUES(@Name,@Role,@Email,@Salary)", con);

            insertCommand.Parameters.Add("@Name", SqlDbType.VarChar, 100, "Name");
            insertCommand.Parameters.Add("@Role", SqlDbType.VarChar, 100, "Role");
            insertCommand.Parameters.Add("@Email", SqlDbType.VarChar, 100, "Email");
            insertCommand.Parameters.Add("@Salary", SqlDbType.Int, 0, "Salary");

            adapter.InsertCommand = insertCommand;

            return adapter.Update(ds, "Employee");
        }

        // ================= UPDATE =================

        static int UpdateRecord()
        {
            Console.WriteLine("Enter Employee Id:");
            int id = Convert.ToInt32(Console.ReadLine());

            Console.WriteLine("Enter new Name:");
            string name = Console.ReadLine();

            Console.WriteLine("Enter new Role:");
            string role = Console.ReadLine();

            Console.WriteLine("Enter new Email:");
            string email = Console.ReadLine();

            Console.WriteLine("Enter new Salary:");
            int salary = Convert.ToInt32(Console.ReadLine());

            using SqlConnection con = new SqlConnection(sqlstring);

            SqlDataAdapter adapter =
                new SqlDataAdapter("SELECT * FROM Employee", con);

            DataSet ds = new DataSet();

            adapter.Fill(ds, "Employee");

            DataTable table = ds.Tables["Employee"];

            DataRow row = table.Rows.Find(id);

            if (row == null)
            {
                Console.WriteLine("Record not found.");
                return 0;
            }

            row["Name"] = name;
            row["Role"] = role;
            row["Email"] = email;
            row["Salary"] = salary;

            SqlCommand updateCommand = new SqlCommand(
                @"UPDATE Employee 
                  SET Name=@Name, Role=@Role, Email=@Email, Salary=@Salary 
                  WHERE Id=@Id", con);

            updateCommand.Parameters.Add("@Name", SqlDbType.VarChar, 100, "Name");
            updateCommand.Parameters.Add("@Role", SqlDbType.VarChar, 100, "Role");
            updateCommand.Parameters.Add("@Email", SqlDbType.VarChar, 100, "Email");
            updateCommand.Parameters.Add("@Salary", SqlDbType.Int, 0, "Salary");

            SqlParameter idParam =
                updateCommand.Parameters.Add("@Id", SqlDbType.Int);

            idParam.SourceColumn = "Id";
            idParam.SourceVersion = DataRowVersion.Original;

            adapter.UpdateCommand = updateCommand;

            return adapter.Update(ds, "Employee");
        }

        // ================= DELETE =================

        static int DeleteRecord()
        {
            Console.WriteLine("Enter Employee Id to Delete:");
            int id = Convert.ToInt32(Console.ReadLine());

            using SqlConnection con = new SqlConnection(sqlstring);

            SqlDataAdapter adapter =
                new SqlDataAdapter("SELECT * FROM Employee", con);

            DataSet ds = new DataSet();

            adapter.Fill(ds, "Employee");

            DataTable table = ds.Tables["Employee"];

            table.PrimaryKey = new DataColumn[] { table.Columns["Id"] };

            DataRow row = table.Rows.Find(id);

            if (row == null)
            {
                Console.WriteLine("Record not found.");
                return 0;
            }

            row.Delete();

            SqlCommand deleteCommand = new SqlCommand(
                "DELETE FROM Employee WHERE Id=@Id", con);

            SqlParameter idParam =
                deleteCommand.Parameters.Add("@Id", SqlDbType.Int);

            idParam.SourceColumn = "Id";
            idParam.SourceVersion = DataRowVersion.Original;

            adapter.DeleteCommand = deleteCommand;

            return adapter.Update(ds, "Employee");
        }
    }
}

```

| Id | Name           | Role               | Email                 | Salary    |
| -- | -------------- | ------------------ | --------------------- | --------- |
| 2  | Sumit Tikone   | Software Developer | sumit@example.com     | 65000.00  |
| 3  | Amit Sharma    | QA Engineer        | amit@example.com      | 55000.00  |
| 4  | Neha Patil     | Database Admin     | neha@example.com      | 72000.00  |
| 5  | Rahul Deshmukh | Web Developer      | rahul@example.com     | 58000.00  |
| 6  | Priya Kulkarni | UI/UX Designer     | priya@example.com     | 60000.00  |
| 7  | Snehal More    | System Analyst     | snehal@example.com    | 68000.00  |
| 8  | Vikram Singh   | Project Manager    | vikram@example.com    | 95000.00  |
| 9  | Pooja Jadhav   | HR Manager         | pooja@example.com     | 50000.00  |
| 10 | Aniket Mane    | Backend Developer  | aniket@example.com    | 63000.00  |
| 11 | Sayali Pawar   | Mobile Developer   | sayali@example.com    | 61000.00  |
| 12 | Manoj Gupta    | Network Engineer   | manoj@example.com     | 57000.00  |
| 13 | Aarti Shah     | Business Analyst   | aarti@example.com     | 64000.00  |
| 14 | saee kavle     | tester             | saeekavle@gmail.com   | 32333.00  |
| 15 | savita kavle   | trainee            | savitakavle@gmail.com | 34444.00  |
| 16 | sumit tikone   | developer          | sumit@gmail.com       | 33333.00  |
| 17 | salman kahan   | manager            | salman@gmail.com      | 123321.00 |
| 18 | sumit          | developer          | sumit@gmail.com       | 15000.00  |
